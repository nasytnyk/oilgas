namespace Oilgas.Measurements;

/// <summary>
/// Свердловина або майданчик (напр. CPF — центральна установка підготовки).
/// Тут — голий ідентифікатор, захардкоджений enum-ом: свідоме спрощення демо.
/// У проді це був би запис у БД зі своїми параметрами.
/// </summary>
public enum Well
{
    W12,
    Cpf,
}
