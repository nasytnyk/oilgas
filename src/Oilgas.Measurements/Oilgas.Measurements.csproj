<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- дані метрик поряд із кодом; копіюються у вивід, звідти їх читає IConfiguration -->
    <Content Include="measurements.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
