<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- словник метрик поряд із кодом; копіюється у вивід, звідти читає IConfiguration -->
    <Content Include="measurements.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
